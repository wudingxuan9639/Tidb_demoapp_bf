# TiDB 操作与排错笔记（2026-08-07）

## 1. 项目与连接信息

本项目由 Vue 3 前端、FastAPI 后端和 TiDB 组成。TiDB 使用 MySQL 协议，因此后端、DBeaver 和 `mysql` 终端客户端都通过 SQL 端口连接。

| 服务 | 地址 | 用途 |
| --- | --- | --- |
| Vue / Vite | `http://127.0.0.1:8517` | 前端开发页面 |
| FastAPI | `http://127.0.0.1:8800` | 后端 API 与 Swagger 文档（`/docs`） |
| TiDB SQL | `127.0.0.1:4000` | SQL 查询、DBeaver、后端数据库连接 |
| TiDB Dashboard | `http://127.0.0.1:2379/dashboard/` | 集群监控、性能与慢查询诊断 |

项目默认连接串：

```text
mysql+aiomysql://root@127.0.0.1:4000/demo_app?charset=utf8mb4
```

当前业务表：

```text
demo_app.items
```

字段为 `id`、`title`、`content`、`created_at`。前端新增的数据会经过 FastAPI 的 `POST /api/items`，由后端提交到 TiDB；不会保存在 `frontend` 或 `backend` 目录中，也不依赖浏览器缓存。

## 2. 服务与端口的关系

浏览器只能直接访问 HTTP 服务；TiDB 的 `4000` 端口不是网页端口，而是 MySQL 协议端口。

```text
浏览器 -- HTTP --> Vite :8517 -- HTTP --> FastAPI :8800 -- MySQL 协议 --> TiDB :4000
浏览器 -- HTTP --> TiDB Dashboard :2379/dashboard/
DBeaver -- MySQL 协议 --> TiDB :4000
```

结论：

- `8517`、`8800`、`2379` 要有对应服务监听，才能在浏览器访问。
- `4000` 要使用 DBeaver、`mysql` 命令或后端程序连接，不能在浏览器地址栏直接打开。
- Dashboard 用于监控，不用于浏览、编辑表中数据。

## 3. 今天遇到的后端启动问题

### 3.1 前端请求 `:8800/api/items` 超时

现象：前端出现 `ERR_CONNECTION_TIMED_OUT`，并可能伴随资源 `404`。

原因：后端未成功启动，`8800` 没有进程监听。进一步排查发现 SQLAlchemy 异步引擎缺少 `greenlet`。

处理：在 `backend/requirements.txt` 中加入：

```text
greenlet>=3.0.0
```

首次建立或依赖变更后，执行：

```bash
cd /Users/liuchangsheng/Documents/Python_demo/backend
source .venv/bin/activate
python -m pip install -r requirements.txt
uvicorn app.main:app --host 127.0.0.1 --port 8800 --reload
```

健康检查：

```bash
curl http://127.0.0.1:8800/health
```

预期结果：

```json
{"status":"ok"}
```

`pip install -r requirements.txt` 不需要每次启动都执行，只在首次创建虚拟环境、依赖文件更新或虚拟环境被重新创建后执行。

### 3.2 后端报 `Unknown database 'demo_app'`

原因：TiDB 服务已启动，但逻辑数据库 `demo_app` 尚未创建。TiUP Playground 只启动集群，不会自动创建项目数据库。

处理：

```bash
mysql -h 127.0.0.1 -P 4000 -u root -e 'CREATE DATABASE IF NOT EXISTS demo_app CHARACTER SET utf8mb4;'
```

后端随后会自动创建 `items` 表。

### 3.3 前端端口不一致

曾发现正在运行的 Vite 实际监听 `5173`，而项目约定端口为 `8517`。需要确认在正确项目目录启动：

```bash
cd /Users/liuchangsheng/Documents/Python_demo/frontend
npm run dev
```

## 4. TiUP Playground、集群与数据持久化

### 4.1 层级关系

```text
一台电脑
└── TiUP Playground 启动的一套 TiDB 集群
    ├── TiDB Server：SQL 入口，例如 :4000
    ├── PD：集群协调与 Dashboard，例如 :2379
    ├── TiKV：实际持久化数据
    └── 多个逻辑数据库
        ├── demo_app
        │   └── items
        └── import_demo
            ├── test_orders
            └── test_orders_excel
```

一个 `tiup playground` 命令启动一套集群；一套集群可以包含多个数据库。一个 TiDB Server 进程只连接一套集群。一台电脑可以启动多套集群，但必须使用不同的数据目录和端口。

### 4.2 `--tag` 的作用

未使用 `--tag` 的 Playground 是临时开发集群。为了在正常 `Ctrl + C` 停止后保留同一套数据目录，后续应固定使用相同版本和相同 tag，例如：

```bash
tiup playground v8.5.7 --tag demo-app
```

以后继续使用同一套集群，也要重复使用该命令：

```bash
tiup playground v8.5.7 --tag demo-app
```

对应数据目录：

```text
~/.tiup/data/demo-app
```

不带 tag 再执行 `tiup playground` 会启动新的临时集群，不会自动复用 `demo-app`。

`nightly` 是每日构建的开发版本，适合测试新功能；本地学习与数据保留建议使用固定稳定版本。

### 4.3 今天发现的数据目录情况

本机找到的 Playground 数据目录为：

```text
~/.tiup/data/VRbEoGh
```

内部包含：

```text
tikv-0/data    # TiKV 原始业务数据
pd-0/data      # PD 集群元数据
```

TiKV 不会创建名为 `demo_app` 的普通文件夹；不能通过 Finder、Dashboard 或 DBeaver 直接读取、修改原始 TiKV 文件。

日志显示当前目录在今天重新初始化，且当前集群中没有昨天的 `demo_app`。昨天启动 Playground 时未使用 tag；如果没有 SQL/CSV/Dumpling/Time Machine 备份，也没有另一处保留的数据目录，则昨天的数据无法从当前集群恢复。

今后应在停止本地集群前先做逻辑备份：

```bash
tiup dumpling -h 127.0.0.1 -P 4000 -u root -B demo_app -o ./backup_demo_app
```

## 5. DBeaver 中查看与管理数据

TiDB 在 DBeaver 中选择 MySQL 驱动：

```text
Host: 127.0.0.1
Port: 4000
Username: root
Password: 留空（本地 Playground 默认）
```

查看项目数据：

```text
连接 → Databases → demo_app → Tables → items
```

右键 `items` 选择查看/编辑数据，或在 SQL Editor 中执行：

```sql
SELECT * FROM demo_app.items ORDER BY id DESC;
```

终端中也可查询：

```bash
mysql -h 127.0.0.1 -P 4000 -u root -e 'SELECT * FROM demo_app.items ORDER BY id DESC;'
```

## 6. SQL 脚本导入练习

生成的练习脚本创建：

```text
import_demo.test_orders
```

脚本中包含建库、建表与 5 行测试数据。DBeaver 中通过连接右键菜单打开 `SQL Editor -> New SQL Script`，再粘贴或打开 SQL 文件。脚本标签必须绑定到实际数据源，例如 `<demo_app> Script`；标签显示 `<none>` 时不能执行。

### 6.1 `No database selected`

原因：执行 `CREATE TABLE test_orders` 时，没有先成功执行 `USE import_demo`。

更稳妥的写法是全限定表名：

```sql
CREATE TABLE import_demo.test_orders (...);
INSERT INTO import_demo.test_orders (...);
```

### 6.2 `client has multi-statement capability disabled`

原因：一次将 `DROP`、`CREATE`、`INSERT` 等多条 SQL 作为同一请求发送，TiDB 的安全设置拒绝多语句请求。

处理：不要开启全局 `tidb_multi_statement_mode`。在 DBeaver 中分三次执行：

1. `DROP TABLE IF EXISTS ...;`
2. 完整的 `CREATE TABLE ...;`
3. 完整的 `INSERT INTO ... VALUES (...), (...);`

五行 `VALUES` 属于同一条 `INSERT`，可一次执行。

### 6.3 `Unknown table 'import_demo.test_orders'`

原因：建表语句没有成功执行，因此插入或查询时找不到表。

先确认：

```sql
SHOW TABLES FROM import_demo;
```

出现 `test_orders` 后，再执行插入与查询。

## 7. Excel / CSV 导入练习

练习工作簿包含两个工作表：

| 工作表 | 用途 |
| --- | --- |
| `test_orders` | 含标题与说明，适合查看，不应直接导出为 CSV 导入。 |
| `import_data` | 第一行即字段名，包含 5 行数据，专供导入。 |

正确流程：

1. 打开 Excel，选择 `import_data` 工作表。
2. 确认第一行是 `order_id`、`customer_code`、`customer_name` 等字段名。
3. 另存为 `CSV UTF-8（逗号分隔）`。Excel 提示仅保存当前工作表时，选择继续。
4. 在 DBeaver 中为 Excel 导入准备独立目标表，避免与 SQL 脚本数据发生主键重复：

   ```sql
   CREATE TABLE import_demo.test_orders_excel LIKE import_demo.test_orders;
   ```

5. 右键 `test_orders_excel` -> `Tools` -> `Import Data`，选择 CSV。
6. 设置编码为 UTF-8，并将第一行设为表头。
7. 在 Tables mapping 展开 CSV 左侧小箭头，检查列映射。
8. 执行导入，并验证行数。

### 7.1 导出错误工作表导致 `Column2`、`Column3`

现象：映射页第一列显示“TiDB 测试订单导入数据”，其余显示 `Column2`、`Column3`。

原因：误将带标题的 `test_orders` 工作表保存为 CSV。DBeaver 将标题行而不是字段名识别为表头。

处理：关闭导入向导，重新从 `import_data` 工作表导出 CSV；不需要手动填写 `Column2` 等映射。

### 7.2 `Field 'order_id' doesn't have a default value`

原因：目标表的 `order_id` 是非空主键，CSV 的第一列没有映射到目标 `order_id`，导致插入语句未写入主键。

处理：停止当前导入，不要选择 `Skip`。重新导入后检查：

```text
order_id      -> order_id
customer_code -> customer_code
customer_name -> customer_name
product_name  -> product_name
quantity      -> quantity
unit_price    -> unit_price
order_date    -> order_date
status        -> status
notes         -> notes
```

CSV UTF-8 的首字段有时可能带不可见 BOM；手动将该源列映射到目标 `order_id` 即可。不要通过给主键设置默认值或自动递增来掩盖映射错误。

导入后验证：

```sql
SELECT COUNT(*) FROM import_demo.test_orders_excel;
SELECT * FROM import_demo.test_orders_excel;
```

## 8. 当前操作清单

- [ ] 使用固定 tag 启动 Playground，例如 `tiup playground v8.5.7 --tag demo-app`。
- [ ] 启动后确认 `demo_app` 存在，再启动后端与前端。
- [ ] 将 `test_orders` 的 SQL 建表与插入语句分开执行，确认 `import_demo.test_orders` 有 5 行数据。
- [ ] 从 Excel 的 `import_data` 工作表重新导出 CSV UTF-8。
- [ ] 在 DBeaver 中重新导入 CSV，并将 `order_id` 正确映射到主键列。
- [ ] 验证 `test_orders_excel` 的行数为 5。
- [ ] 在结束本地集群前，为重要数据库使用 Dumpling 导出备份。

## 9. 当前导入状态与下一步

### 9.1 当前现象

在 DBeaver 中已经可以看到：

```text
import_demo
├── test_orders
└── test_orders_excel
```

但 `test_orders_excel` 的数据视图显示 `No data`。这不表示 TiDB 或 DBeaver 连接失败；它表示 CSV 导入任务没有成功插入任何记录。

已定位的直接原因是 CSV 来源错误：导入向导显示的第一列为“TiDB 测试订单导入数据”，而不是 `order_id`。这表示 CSV 来自带标题的 `test_orders` 工作表，导致 DBeaver 识别出 `Column2`、`Column3` 等伪字段，无法自动匹配目标表列。

### 9.2 本次练习文件

| 文件 | 位置 | 用途 |
| --- | --- | --- |
| `tidb_test_orders.sql` | `.codex-tmp/tidb-import-samples/` | 创建 `import_demo`、`test_orders` 与 5 行测试数据。 |
| `tidb_test_orders.xlsx` | `.codex-tmp/tidb-import-samples/` | 含两张工作表；从 `import_data` 导出 CSV。 |

桌面复制曾因系统授权服务暂时不可用而未完成；上述文件仍保存在项目目录中。

### 9.3 重新导入 CSV 的推荐步骤

1. 打开 `tidb_test_orders.xlsx`，点击底部的 `import_data` 工作表。
2. 第一行必须直接显示字段名，第一列必须是 `order_id`。
3. 在 Excel 中选择“另存为”，格式选择 `CSV UTF-8（逗号分隔）`；确认保存的是当前工作表。
4. 在 DBeaver 中右键 `import_demo.test_orders_excel`，选择 `Tools -> Import Data`。
5. 选择新导出的 CSV，设置编码为 UTF-8，并将第一行识别为表头。
6. 在 `Tables mapping` 页面点击 CSV 文件左侧的小箭头，确认 `order_id -> order_id`，其余八列也一一对应。
7. 点击 `Proceed`，在 `Data load settings` 中保持批量导入的默认设置。
8. 点击 `Start`。若有错误，不要选择 `Skip` 或 `Skip all`；点击 `Stop` 后，在 `Database Tasks` 中查看完整错误。

### 9.4 导入后必须执行的验证

```sql
SHOW TABLES FROM import_demo;

SELECT COUNT(*) AS sql_import_count
FROM import_demo.test_orders;

SELECT COUNT(*) AS excel_import_count
FROM import_demo.test_orders_excel;

SELECT *
FROM import_demo.test_orders_excel
ORDER BY order_id;
```

预期是 `test_orders_excel` 返回 5 行，订单编号为 `10001` 至 `10005`。若 `COUNT(*)` 为 0，优先检查 CSV 的第一行是否为真实字段名，以及导入向导中的 `order_id` 是否映射到目标主键列。

## 10. 可重复使用的日常操作顺序

### 启动开发环境

终端 1：启动固定标签的 TiDB 集群。

```bash
tiup playground v8.5.7 --tag demo-app
```

终端 2：启动后端。

```bash
cd /Users/liuchangsheng/Documents/Python_demo/backend
source .venv/bin/activate
uvicorn app.main:app --host 127.0.0.1 --port 8800 --reload
```

终端 3：启动前端。

```bash
cd /Users/liuchangsheng/Documents/Python_demo/frontend
npm run dev
```

### 结束前备份与停止

先导出重要数据：

```bash
tiup dumpling -h 127.0.0.1 -P 4000 -u root -B demo_app -o ./backup_demo_app
```

然后回到运行 Playground 的终端，按 `Ctrl + C` 正常停止。之后继续使用同一个 `--tag demo-app` 启动，才能复用相同的数据目录。
